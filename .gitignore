# Python
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Obsidian vault workspace state (docs/ are an Obsidian vault; see docs/project-plan/process.md)
.obsidian/

# Xcode
xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/

# Bundled Python (rebuilt by packaging/build.sh). No trailing slash: worktrees
# symlink this from the main checkout, and `dir/` patterns don't match symlinks.
PythonResources
packaging/.cache/

# Release artifacts produced by packaging/release.sh
build/
